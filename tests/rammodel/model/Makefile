SIM_TOP := rammodel_test

SIM_SRCS += test.v

COCOTB_MODULE := testbench

IVL_ARGS += -DSIM_LOG
#IVL_ARGS += -DSIM_LOG_FIFO
#IVL_ARGS += -DSIM_LOG_RAMMODEL_STATE_LSU_INS
#IVL_ARGS += -DSIM_LOG_RAMMODEL_STATE_LSU_MC_STATE

include ../../common.mk
